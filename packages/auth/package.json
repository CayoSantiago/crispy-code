{
  "name": "@repo/auth",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "auth:generate": "auth generate --config src/cli.ts"
  },
  "dependencies": {
    "@better-auth/prisma-adapter": "catalog:",
    "@repo/db": "workspace:*",
    "@repo/email": "workspace:*",
    "@repo/env": "workspace:*",
    "better-auth": "catalog:",
    "next": "catalog:",
    "react": "catalog:",
    "server-only": "catalog:"
  },
  "devDependencies": {
    "@repo/biome-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/react": "catalog:",
    "typescript": "catalog:"
  },
  "exports": {
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "imports": {
    "#config": "./src/config.ts"
  }
}
