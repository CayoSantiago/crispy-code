{
  "name": "@repo/email",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "email:dev": "email dev --dir ./src/templates --port 3001"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/env": "workspace:*",
    "@repo/jobs": "workspace:*",
    "@repo/observability": "workspace:*",
    "inngest": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-email": "^6.0.0",
    "resend": "^6.9.2",
    "server-only": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/biome-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "typescript": "catalog:"
  },
  "exports": {
    "./functions": {
      "default": "./src/functions.ts"
    },
    "./schemas": {
      "default": "./src/schema.ts"
    },
    "./enqueue": {
      "default": "./src/enqueue.ts"
    }
  },
  "imports": {
    "#*": [
      "./src/*",
      "./src/*.ts",
      "./src/*.tsx",
      "./src/*/index.ts",
      "./src/*/index.tsx"
    ]
  }
}
